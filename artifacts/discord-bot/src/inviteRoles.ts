import type { Client, Guild, GuildMember } from 'discord.js';
import { loadGuild, updateGuild } from './storage.js';
const inviteUses = new Map<string, Map<string, number>>();
async function snapshotGuild(guild: Guild): Promise<void>{try{const invites=await guild.invites.fetch();const map=new Map<string,number>();for(const invite of invites.values())map.set(invite.code,invite.uses??0);inviteUses.set(guild.id,map);}catch{}}
export async function primeInviteCache(client: Client<true>): Promise<void>{await Promise.all(client.guilds.cache.map(snapshotGuild));}
async function syncRoles(member:GuildMember,count:number):Promise<void>{const mappings=loadGuild(member.guild.id).config.inviteRoles??{};const thresholds=Object.entries(mappings).map(([n,id])=>[Number(n),id] as const).filter(([n,id])=>Number.isInteger(n)&&n>0&&/^\d+$/.test(id)).sort((a,b)=>a[0]-b[0]);for(const [n,id] of thresholds){if(count>=n)await member.roles.add(id).catch(()=>undefined);else await member.roles.remove(id).catch(()=>undefined);}}
export async function handleInviteRole(member:GuildMember):Promise<void>{try{const invites=await member.guild.invites.fetch();const previous=inviteUses.get(member.guild.id)??new Map<string,number>();let usedBy:string|undefined;for(const invite of invites.values()){if((invite.uses??0)>(previous.get(invite.code)??0)){usedBy=invite.inviter?.id;break;}}const current=new Map<string,number>();for(const invite of invites.values())current.set(invite.code,invite.uses??0);inviteUses.set(member.guild.id,current);if(!usedBy)return;updateGuild(member.guild.id,d=>{d.invites??={};d.invites[usedBy!]=(d.invites[usedBy!]??0)+1;});const inviter=await member.guild.members.fetch(usedBy).catch(()=>null);if(inviter)await syncRoles(inviter,loadGuild(member.guild.id).invites?.[usedBy]??0);}catch(err){console.error('[inviteRoles]',err);}}
export async function getInviteCount(guildId:string,userId:string):Promise<number>{return loadGuild(guildId).invites?.[userId]??0;}
