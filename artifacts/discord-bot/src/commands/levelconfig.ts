import { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder, ChannelType } from 'discord.js';
import type { Command } from '../types.js';
import { loadGuild, updateGuild } from '../storage.js';
const defaultLeveling=()=>({xpMin:15,xpMax:25,cooldownSeconds:60,multiplier:1,bonusXp:0,levelUp:{},ignoreChannels:[] as string[],ignoreRoles:[] as string[],roleRewards:{} as Record<string,string>});
export const levelconfig:Command={data:new SlashCommandBuilder().setName('levelconfig').setDescription('Configure the server leveling system (admin only)').setDefaultMemberPermissions(PermissionFlagsBits.Administrator)
.addSubcommand(s=>s.setName('set').setDescription('Set XP gain, cooldown, multiplier and bonus XP').addIntegerOption(o=>o.setName('xp_min').setDescription('Minimum random XP').setMinValue(0).setMaxValue(1000)).addIntegerOption(o=>o.setName('xp_max').setDescription('Maximum random XP').setMinValue(0).setMaxValue(1000)).addIntegerOption(o=>o.setName('cooldown').setDescription('XP cooldown in seconds').setMinValue(0).setMaxValue(86400)).addNumberOption(o=>o.setName('multiplier').setDescription('XP multiplier').setMinValue(0).setMaxValue(100)).addIntegerOption(o=>o.setName('bonus_xp').setDescription('Flat bonus XP').setMinValue(0).setMaxValue(10000)))
.addSubcommand(s=>s.setName('message').setDescription('Customize level-up announcements').addStringOption(o=>o.setName('title').setDescription('Title; {user}, {level}, {xp}').setMaxLength(256)).addStringOption(o=>o.setName('description').setDescription('Description; {user}, {level}, {xp}').setMaxLength(4000)).addStringOption(o=>o.setName('image').setDescription('Image/GIF URL')).addStringOption(o=>o.setName('color').setDescription('Hex color')).addChannelOption(o=>o.setName('channel').setDescription('Announcement channel').addChannelTypes(ChannelType.GuildText,ChannelType.GuildAnnouncement)))
.addSubcommand(s=>s.setName('reward').setDescription('Give a role at a level').addIntegerOption(o=>o.setName('level').setDescription('Level').setRequired(true).setMinValue(1).setMaxValue(10000)).addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true)))
.addSubcommand(s=>s.setName('removereward').setDescription('Remove a level role reward').addIntegerOption(o=>o.setName('level').setDescription('Level').setRequired(true).setMinValue(1).setMaxValue(10000)))
.addSubcommand(s=>s.setName('ignorechannel').setDescription('Toggle XP in a channel').addChannelOption(o=>o.setName('channel').setDescription('Channel').setRequired(true).addChannelTypes(ChannelType.GuildText,ChannelType.GuildAnnouncement)))
.addSubcommand(s=>s.setName('ignorerole').setDescription('Toggle XP for a role').addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true)))
.addSubcommand(s=>s.setName('view').setDescription('View leveling configuration')).addSubcommand(s=>s.setName('reset').setDescription('Reset leveling configuration')).addSubcommand(s=>s.setName('preview').setDescription('Preview level-up announcement')),
async execute(interaction){if(!interaction.guild)return;await interaction.deferReply({ephemeral:true});const sub=interaction.options.getSubcommand();if(sub==='reset'){updateGuild(interaction.guild.id,d=>{d.config.leveling=defaultLeveling();delete d.config.levelUpMessage;});await interaction.editReply('✅ Leveling configuration reset.');return;}if(sub==='set'){const min=interaction.options.getInteger('xp_min'),max=interaction.options.getInteger('xp_max'),cool=interaction.options.getInteger('cooldown'),mult=interaction.options.getNumber('multiplier'),bonus=interaction.options.getInteger('bonus_xp');const cur=loadGuild(interaction.guild.id).config.leveling??defaultLeveling();if(min===null&&max===null&&cool===null&&mult===null&&bonus===null){await interaction.editReply('❌ Provide at least one setting.');return;}const nmin=min??cur.xpMin,nmax=max??cur.xpMax;if(nmin>nmax){await interaction.editReply('❌ xp_min cannot be greater than xp_max.');return;}updateGuild(interaction.guild.id,d=>{const c=d.config.leveling??defaultLeveling();c.xpMin=nmin;c.xpMax=nmax;if(cool!==null)c.cooldownSeconds=cool;if(mult!==null)c.multiplier=mult;if(bonus!==null)c.bonusXp=bonus;d.config.leveling=c;});await interaction.editReply(`✅ Leveling updated: **${nmin}-${nmax} XP**, **${cool??cur.cooldownSeconds}s** cooldown, **${(mult??cur.multiplier).toFixed(2)}x** multiplier, **+${bonus??cur.bonusXp}** bonus XP.`);return;}if(sub==='message'){const title=interaction.options.getString('title'),description=interaction.options.getString('description'),image=interaction.options.getString('image'),raw=interaction.options.getString('color'),channel=interaction.options.getChannel('channel');if(!title&&!description&&!image&&!raw&&!channel){await interaction.editReply('❌ Provide at least one announcement setting.');return;}let color:number|undefined;if(raw){color=parseColor(raw);if(color===undefined){await interaction.editReply('❌ Invalid color.');return;}}if(image){try{const u=new URL(image);if(!['http:','https:'].includes(u.protocol))throw new Error();}catch{await interaction.editReply('❌ Image must be an HTTP/HTTPS URL.');return;}}updateGuild(interaction.guild.id,d=>{const c=d.config.leveling??defaultLeveling();c.levelUp=c.levelUp??{};if(title!==null)c.levelUp.title=title??undefined;if(description!==null)c.levelUp.description=description??undefined;if(image!==null)c.levelUp.imageUrl=image??undefined;if(raw!==null)c.levelUp.color=color;if(channel!==null)c.levelUp.channelId=channel?.id;d.config.leveling=c;d.config.levelUpMessage={title:c.levelUp.title,description:c.levelUp.description,imageUrl:c.levelUp.imageUrl};});await interaction.editReply('✅ Level-up announcement customization saved.');return;}if(sub==='reward'||sub==='removereward'){const level=interaction.options.getInteger('level',true);if(sub==='reward'){const role=interaction.options.getRole('role',true);if(role.managed){await interaction.editReply('❌ Managed roles cannot be granted.');return;}updateGuild(interaction.guild.id,d=>{const c=d.config.leveling??defaultLeveling();c.roleRewards={...(c.roleRewards??{}),[String(level)]:role.id};d.config.leveling=c;d.config.levelRoles={...(d.config.levelRoles??{}),[String(level)]:role.id};});await interaction.editReply(`✅ Level **${level}** now rewards ${role}.`);}else{updateGuild(interaction.guild.id,d=>{const c=d.config.leveling??defaultLeveling();delete c.roleRewards[String(level)];d.config.leveling=c;if(d.config.levelRoles)delete d.config.levelRoles[String(level)];});await interaction.editReply(`✅ Removed the role reward for level **${level}**.`);}return;}if(sub==='ignorechannel'||sub==='ignorerole'){const id=sub==='ignorechannel'?interaction.options.getChannel('channel',true).id:interaction.options.getRole('role',true).id;let added=false;updateGuild(interaction.guild.id,d=>{const c=d.config.leveling??defaultLeveling();const key=sub==='ignorechannel'?'ignoreChannels':'ignoreRoles',list=c[key]??[],idx=list.indexOf(id);if(idx>=0)list.splice(idx,1);else{list.push(id);added=true;}c[key]=list;d.config.leveling=c;});await interaction.editReply(`${added?'🚫 Added':'✅ Removed'} ${sub==='ignorechannel'?'that channel':'that role'} ${added?'from':'from'} XP tracking.`);return;}const c=loadGuild(interaction.guild.id).config.leveling??defaultLeveling();if(sub==='view'){const e=new EmbedBuilder().setColor(c.levelUp?.color??0x9B59B6).setTitle('📈 Leveling Configuration').addFields({name:'XP Gain',value:`${c.xpMin}-${c.xpMax}`,inline:true},{name:'Cooldown',value:`${c.cooldownSeconds}s`,inline:true},{name:'Multiplier',value:`${c.multiplier}x`,inline:true},{name:'Bonus XP',value:`+${c.bonusXp}`,inline:true},{name:'Ignored Channels',value:String(c.ignoreChannels.length),inline:true},{name:'Ignored Roles',value:String(c.ignoreRoles.length),inline:true},{name:'Role Rewards',value:String(Object.keys(c.roleRewards??{}).length),inline:true}).setTimestamp();await interaction.editReply({embeds:[e]});return;}await interaction.editReply({content:'**Level-up preview**',embeds:[buildLevelUpEmbed(interaction.user.toString(),7,1200,interaction.user.displayAvatarURL(),c.levelUp?.title,c.levelUp?.description,c.levelUp?.imageUrl,c.levelUp?.color)]});}};
function parseColor(value:string):number|undefined{const clean=value.trim().replace(/^#/,'');return/^[0-9a-f]{6}$/i.test(clean)?parseInt(clean,16):undefined;}
export function buildLevelUpEmbed(userMention:string,level:number,xp:number,avatarUrl:string,customTitle?:string,customDescription?:string,customImageUrl?:string,customColor?:number):EmbedBuilder{const r:Record<string,string>={'{user}':userMention,'{level}':String(level),'{xp}':xp.toLocaleString()};const apply=(s:string)=>Object.entries(r).reduce((out,[k,v])=>out.replaceAll(k,v),s);const e=new EmbedBuilder().setColor(customColor??0x9B59B6).setTitle(customTitle?apply(customTitle):'🎉 Level Up!').setDescription(customDescription?apply(customDescription):`Congratulations ${userMention}!\nYou reached **level ${level}**.`).setThumbnail(avatarUrl).setTimestamp();if(customImageUrl)e.setImage(customImageUrl);return e;}
