{
  "name": "@workspace/discord-bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm install --filter @workspace/discord-bot... --no-frozen-lockfile && pnpm exec tsx src/index.ts",
    "start": "pnpm exec tsx src/index.ts",
    "deploy": "pnpm install --filter @workspace/discord-bot... --no-frozen-lockfile && pnpm exec tsx src/deploy.ts",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@discordjs/voice": "^0.19.2",
    "@distube/ytdl-core": "^4.16.12",
    "@neondatabase/serverless": "^1.0.2",
    "discord.js": "^14.27.0",
    "opusscript": "^0.1.1",
    "play-dl": "^1.9.7",
    "prism-media": "^1.3.5"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "~5.9.3"
  }
}
